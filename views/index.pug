extends layout

append content
  div(class="px-12 py-12 my-6 mx-12 text-center dark:bg-gray-900 rounded-full")
    h1(class="text-4xl font-bold leading-none sm:text-5xl") Weeduino
    span(class="dark:text-emerald-400")  Cloud's
    span  Control
  div(class="invisible flex flex-col items-center justify-center w-100" id="loading")
    +load(class="w-16 h-16 mb-6 border-4 border-dashed rounded-full animate-spin dark:border-violet-800")
    +snackbar("Loading ", "PLease Wait", "info")

append head 
  script(type="module" src="/javascripts/app.js").
